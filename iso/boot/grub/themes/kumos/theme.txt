# KumOS GRUB theme - modern centered layout (Fedora/GNOME boot-screen
# style): dark radial-glow background, centered wordmark, centered rounded
# menu cards. No live "__timeout__" countdown widget - GRUB 2.14 renders
# it frozen at "0" regardless of the real timeout value (confirmed with
# both a progress_bar and a %d-format label, top-level and nested, and a
# 300s timeout that never visibly ticked down) - a real upstream/version
# quirk, not a theme bug, so a static hint is used instead of a number
# that would always read as wrong.

title-text: ""
desktop-image: "background.png"
desktop-color: "#05060a"

+ vbox {
    top = 14%
    left = 20%
    width = 60%
    height = 90

    + label {
        text = "KumOS"
        font = "KumOS Title"
        color = "#f5f7fa"
        align = "center"
    }

    + vbox { height = 10 }

    + label {
        text = "Choose an operating system to start"
        font = "KumOS Text"
        color = "#8b96a8"
        align = "center"
    }
}

+ boot_menu {
    left = 27%
    top = 38%
    width = 46%
    height = 230
    item_height = 52
    item_padding = 14
    item_spacing = 18
    item_icon_space = 20
    icon_width = 30
    icon_height = 30
    icon_dir = "icons"
    item_font = "KumOS Menu"
    selected_item_font = "KumOS Menu"
    item_color = "#aab4c4"
    selected_item_color = "#ffffff"
    selected_item_pixmap_style = "select_*.png"
}

+ label {
    top = 86%
    left = 30%
    width = 40%
    text = "Starting KumOS automatically..."
    font = "KumOS Text"
    color = "#5b6c88"
    align = "center"
}

+ label {
    top = 95%
    left = 10%
    width = 80%
    text = "enter select    e edit    c command line"
    font = "KumOS Text"
    color = "#3f4a5c"
    align = "center"
}
