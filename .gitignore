# Build artifacts - regenerated by `make`. kumos.bin and kumos.iso (the
# actual shippable outputs) stay tracked; everything below is either an
# intermediate object file or a byte-identical staging copy already baked
# into kumos.iso.
*.o
iso/boot/kumos.bin
iso/boot/grub/grub.cfg
